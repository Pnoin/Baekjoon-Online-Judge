#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	string a;
	cin >> a;
	cout << ":fan::fan::fan:" << "\n";
	cout << ":fan::" << a << "::fan:" << "\n";
	cout << ":fan::fan::fan:";	
}